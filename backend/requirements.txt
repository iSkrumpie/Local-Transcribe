# Local Transcribe — backend dependencies
# Tested on Python 3.11 across Linux / macOS / Windows.

# --- web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0

# --- cross-platform STT backend (CPU / CUDA) ---
faster-whisper>=1.0.3,<2.0
# Note: faster-whisper pulls in CTranslate2, tokenizers, huggingface-hub.
# Optional CUDA acceleration: install the matching CUDA toolkit + cuDNN yourself.

# --- Apple-Silicon STT backend (macOS arm64 only) ---
# Uncomment on macOS Apple Silicon for the GPU-accelerated path (much faster):
# mlx-whisper>=0.4.0
